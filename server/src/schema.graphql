scalar Date

type Brand {
  id: ID!,
  name: String!,
  createdAt: Date!,
  type: [String!]!,
  country: String!,
  description: String,
}

type Query {
  brands: [Brand!]!,
  brand(name: String!): Brand!
}

type Mutation {
  createBrand(name: String!, createdAt: Date!, type: [String!]!, country: String!, description: String): Brand!
  updateBrand(id: ID!, name: String!, createdAt: Date!, type: [String!]!, country: String!, description: String): Brand!
  deleteBrand(id: ID!): Brand!
}